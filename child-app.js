const WEBHOOK='https://hook.eu2.make.com/qhf8gxv7v9pjuonoy63f2jar53aqg94n';
const key=CHILD.toLowerCase();
const points=document.getElementById('points'),screen=document.getElementById('screen'),tasks=document.getElementById('tasks'),message=document.getElementById('message');
const escapeHtml=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(data){const child=data.children[key];points.textContent=child.points??0;screen.textContent=`Screen time: ${child.screenTime||'Yes'}`;screen.classList.toggle('no',child.screenTime==='No');const items=child.outstandingTasks||[];tasks.innerHTML=items.length?items.map(t=>`<article class="task"><div class="task-name">${escapeHtml(typeof t==='string'?t:t.title)}</div><button class="complete" type="button" data-task-id="${escapeHtml(t.id||'')}" ${t.id?'':'disabled'}>Complete</button></article>`).join(''):'<div class="empty">Everything is done ✓</div>';}
async function load(){try{const r=await fetch(`latest.json?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error('Could not load dashboard');render(await r.json());message.textContent='';}catch(e){message.textContent=e.message;message.className='message error';}}
tasks.addEventListener('click',async event=>{const button=event.target.closest('button[data-task-id]');if(!button)return;button.disabled=true;button.textContent='Saving…';message.textContent='';try{const body=new URLSearchParams({taskId:button.dataset.taskId,child:CHILD});const r=await fetch(WEBHOOK,{method:'POST',body});if(!r.ok)throw new Error('Could not complete task');const data=await r.json();button.textContent='Done';button.classList.add('done');render(data);}catch(e){button.disabled=false;button.textContent='Complete';message.textContent=e.message;message.className='message error';}});
load();setInterval(load,60000);
