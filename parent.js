const WEBHOOK='https://hook.eu2.make.com/9otc8coig6ulfw6eeqt8oraph8rhi4y1';
const els={rassell:document.getElementById('rassellTasks'),michelle:document.getElementById('michelleTasks')};
const message=document.getElementById('message');
const escapeHtml=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function taskHtml(t,child){return `<article class="task"><div><div class="task-name">${escapeHtml(t.title)}</div><small>+${Number(t.points||0)} points</small></div><button class="reset" type="button" data-task-id="${escapeHtml(t.id)}" data-child="${child}">Reset</button></article>`;}
function render(data){let count=0;for(const child of ['rassell','michelle']){const items=data.children?.[child]?.completedTasks||[];count+=items.length;els[child].innerHTML=items.length?items.map(t=>taskHtml(t,child==='rassell'?'Rassell':'Michelle')).join(''):'<div class="empty">No completed tasks</div>';}document.getElementById('completedCount').textContent=count;document.getElementById('updated').textContent=data.updatedAt?new Date(data.updatedAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}):'—';}
async function load(){try{const r=await fetch(`latest.json?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error('Could not load dashboard');render(await r.json());message.textContent='';}catch(e){message.textContent=e.message;message.className='message error';}}
document.addEventListener('click',async e=>{const button=e.target.closest('button.reset');if(!button)return;if(!confirm('Reset this task and reverse its points?'))return;const taskId=button.dataset.taskId,child=button.dataset.child;button.disabled=true;button.textContent='Resetting…';message.textContent='';try{const r=await fetch(WEBHOOK,{method:'POST',body:new URLSearchParams({taskId,child})});if(!r.ok)throw new Error('Reset failed');const data=await r.json();render(data);message.textContent='Task reopened and points reversed.';message.className='message success';}catch(err){button.disabled=false;button.textContent='Reset';message.textContent='Reset failed. Try again.';message.className='message error';}});
load();setInterval(load,60000);